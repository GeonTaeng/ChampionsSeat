# PRD: KIA 챔피언스 필드 좌석 추천 서비스

- 문서 유형: Product Requirements Document (AI 코딩 도구용 구현 명세)
- 화면 수: 1개 (단일 화면)
- 기능 수: 1개 (조건 입력 → TOP 3 좌석 추천)
- 제외 범위: 로그인/회원가입, 결제/예매 연동, DB 연동
- 기술스택: 미지정 (구현 도구가 자유롭게 선택)

---

## 1) 목표와 성공 조건

### 목표
예매 초보자·원정 팬이 응원팀, 방문일, 인원, 예산, 관람 선호도를 입력하면 조건에 맞는 최적 좌석 TOP 3를 즉시 추천받아, 좌석 정보를 직접 비교·분석하는 시간을 없앤다.

### 성공 조건 (Definition of Success)
1. 좌석 구역별 평일/주말 가격 및 블록 정보가 실제 예매 기준 데이터와 100% 일치한다.
2. 입력한 예산으로 구매 가능한 좌석이 하나도 없을 경우, 시스템 에러가 아닌 "예산 초과 안내 + 최소 필요 예산 가이드" 메시지를 노출한다.
3. 사용자가 입력 완료 후 추천 결과까지 도달하는 데 별도 설명 없이도 조작 가능해야 한다 (직관적 단일 화면).
4. 결과에는 반드시 순위별 좌석 정보(구분·블록·예상 총금액)와 추천 이유가 함께 표시된다.

### 비목표 (Out of Scope) — 기획서 "안 만들 것" 반영
- 로그인, 회원가입
- DB 연동
- 결제·티켓 구매·예매 연동
- 실시간 잔여 좌석 연동 API
- 좌석 선택 후 예매 페이지 이동
- AI 챗봇형 추가 질의응답
- 전체 좌석 인터랙티브 좌석도 조작
- 추천 이력 저장·즐겨찾기

---

## 2) 사용자 스토리

| # | 사용자 스토리 | 완료 기준 |
|---|---|---|
| US-1 | 나는 야구장 초보 관람객으로서, 복잡한 좌석 구조를 몰라도 조건만 입력하면 어디 앉을지 추천받고 싶다. | 5개 입력 항목만으로 추천 결과를 확인할 수 있다. |
| US-2 | 나는 원정 팬으로서, 응원팀이 홈/어웨이인지에 따라 다른 추천 결과를 받고 싶다. | 응원팀 선택값이 추천 로직에 반영된다. |
| US-3 | 나는 예산이 한정된 사용자로서, 내 예산 안에서 살 수 있는 좌석만 추천받고 싶다. | 예산 초과 좌석은 추천 결과에서 제외된다. |
| US-4 | 나는 예산이 부족한 사용자로서, 예산이 부족하면 그 이유와 최소 얼마가 더 필요한지 알고 싶다. | 예산 초과 시 전용 안내 메시지와 최소 필요 예산이 표시된다. |
| US-5 | 나는 시야/편의/응원 중 무엇을 중시하는지 스스로 정할 수 있어야 한다. | 3개 선호도 항목을 각각 0~5점으로 조절할 수 있다. |
| US-6 | 나는 추천받은 좌석이 왜 추천됐는지 이해하고 싶다. | TOP 3 각각에 추천 이유 텍스트가 표시된다. |

---

## 3) 화면 명세 (단일 화면)

화면은 위에서 아래로 **[입력 영역] → [실행 버튼] → [결과 영역]** 한 화면 안에서 스크롤로 이어지는 구조. 페이지 이동 없음.

### 3-1. 입력 영역 (Input Section)

| 항목 | 필드명 | 타입 | 옵션/범위 | 필수 | 기본값 |
|---|---|---|---|---|---|
| ① 응원팀 | cheer_team | 라디오 버튼 | 홈(KIA) / 어웨이 | Y | 미선택 |
| ② 방문일 유형 | visit_day_type | 라디오 버튼 | 평일 / 주말(공휴일 포함) | Y | 미선택 |
| ③ 방문 인원 | party_size | 숫자 입력 (스테퍼) | 1~40 정수 | Y | 1 |
| ④ 총 예산 | total_budget | 숫자 입력 | 0 이상 정수, 원 단위 | Y | 공란 |
| ⑤-1 응원 선호도 | pref_cheer | 슬라이더 | 0~5 정수 | Y | 3 |
| ⑤-2 시야 선호도 | pref_view | 슬라이더 | 0~5 정수 | Y | 3 |
| ⑤-3 편의 선호도 | pref_comfort | 슬라이더 | 0~5 정수 | Y | 3 |

- 입력 필드 옆에는 각 항목이 무엇을 의미하는지 1줄 설명(placeholder 또는 helper text)을 둔다.
  - 예: "총 예산 — 인원 전체가 지불할 좌석 총액을 입력하세요 (원)"

### 3-2. 실행 버튼

- 버튼 라벨: `좌석 추천받기`
- 클릭 시 입력값 유효성 검증 → 통과 시 추천 로직 실행 → 결과 영역 렌더링
- 검증 실패/처리 중/실패 상태에 따라 버튼 및 화면 상태가 아래 "4) 기능 명세", "5) 예외 처리"를 따른다.

### 3-3. 결과 영역 (Result Section)

- 기본 상태: 비어 있음 (아직 추천을 실행하지 않았음을 안내하는 플레이스홀더 텍스트만 표시)
- 정상 결과 상태: TOP 3 카드 3개, 순위순 정렬 (1위 → 3위)

각 카드 구성:

| 항목 | 설명 |
|---|---|
| 순위 | 1위 / 2위 / 3위 |
| 구역 구분 | 예: 테이블석, 익사이팅석, 외야 자유석 등 |
| 블록 | 예: 1루 블록 A, 3루 익사이팅존 등 |
| 예상 총금액 | (좌석 1석 가격 × 방문 인원)에 근거한 총액, 원 단위 |
| 추천 이유 | 응원팀·평일/주말·선호도 3항목을 근거로 생성된 1~3문장 설명 |

- 결과 영역 상단에는 입력 요약(응원팀/방문일 유형/인원/예산)을 1줄로 재노출하여 사용자가 자신이 무엇을 입력했는지 다시 확인할 수 있게 한다.
- 예산 초과 등 예외 상태의 화면은 "5) 예외 처리"에서 상세 정의.

---

## 4) 기능 명세

### 기능 1: 조건 기반 좌석 TOP 3 추천 (유일 기능)

**입력**
- cheer_team, visit_day_type, party_size, total_budget, pref_cheer, pref_view, pref_comfort

**사전 준비 데이터 (정적 데이터셋, DB 아님 — 코드/설정 파일 내 하드코딩 또는 JSON 파일)**
- 챔피언스 필드 좌석 구역 마스터 데이터. 각 구역은 아래 속성을 가진다.
  - 구역 구분명, 블록명
  - 평일 가격, 주말 가격 (원/1석)
  - 응원 점수 (0~5): 해당 구역이 응원에 얼마나 유리한지
  - 시야 점수 (0~5): 경기 시야가 얼마나 좋은지
  - 편의 점수 (0~5): 좌석 편안함·이동 편의성
  - 홈/어웨이 팬 적합도 태그 (홈 응원석 / 어웨이 응원 가능 구역 / 중립)
- ※ 성공 조건 1)에 따라 이 데이터는 실제 KIA 챔피언스 필드 예매처 기준 가격·블록 정보와 100% 일치해야 한다. (구현 시 최신 가격표 기준으로 데이터셋 검증 필요)

**처리 로직 (순서)**
1. 입력값 유효성 검증 (5-1 참고)
2. `visit_day_type`에 따라 각 좌석 구역의 적용 가격(평일가/주말가) 결정
3. `cheer_team`이 어웨이인 경우, 홈 응원 전용 구역은 추천 후보에서 제외(또는 응원 점수를 낮게 재계산)
4. 좌석 1석 가격 × `party_size` = 구역별 예상 총금액 계산
5. 예상 총금액이 `total_budget` 이하인 구역만 후보로 필터링
6. 필터링된 후보가 0개면 → 5-2 예외 처리로 분기, 정상 추천 미실행
7. 후보 각각에 대해 매칭 점수 계산:
   - `매칭점수 = (응원 점수 × pref_cheer) + (시야 점수 × pref_view) + (편의 점수 × pref_comfort)`
8. 매칭 점수 내림차순 정렬 후 상위 3개 선정 (동점 시 예상 총금액이 낮은 구역 우선)
9. 각 상위 3개에 대해 추천 이유 텍스트 생성 (아래 규칙 참고)
10. 결과 영역에 TOP 3 카드 렌더링

**추천 이유 생성 규칙 (템플릿 기반, 별도 AI 호출 없이 구현 가능)**
- 입력한 선호도 중 가장 높은 점수 항목 1~2개를 우선 언급
- 예: "시야 선호도가 높아 1루측 정면 시야가 확보되는 블록을 추천드려요. 주말 기준 예상 총금액은 인원 {N}명 기준 {금액}원입니다."
- 어웨이 팬인 경우 원정 응원 가능 구역임을 명시

**출력**
- TOP 3 좌석 카드 (구역 구분, 블록, 예상 총금액, 추천 이유)
- 또는 예외 상태 화면 (5) 예외 처리 참고)

---

## 5) 예외 처리

> 최소 5개 이상, 아래 4가지 항목을 실제 화면 동작까지 구체화하여 정의한다. 모든 예외는 **화면이 깨지거나 빈 화면/무한 로딩으로 끝나지 않도록** 사용자에게 명확한 안내와 다음 행동(재시도/수정)을 제공하는 것을 원칙으로 한다.

### 5-1. 입력이 비어있는 경우 (필수값 미입력)

- **트리거 조건**: `좌석 추천받기` 클릭 시점에 cheer_team, visit_day_type, party_size, total_budget, pref_cheer/view/comfort 중 하나라도 미입력·미선택 상태
- **동작**
  1. 좌석 추천 로직을 실행하지 않는다 (서버/연산 호출 자체를 막는다).
  2. 미입력된 필드 각각에 인라인 에러 메시지 표시 (예: "응원팀을 선택해주세요")
  3. 미입력 필드는 시각적으로 강조(테두리 색 변경 등)하고, 화면을 해당 필드로 자동 스크롤/포커스 이동
  4. 결과 영역은 기존 상태 유지 (빈 화면이면 빈 화면 그대로, 이전 결과가 있으면 이전 결과 유지)
- **부가 규칙**
  - `party_size`가 0 이하이거나 정수가 아닌 경우도 동일하게 "1명 이상의 정수를 입력해주세요" 에러로 처리
  - `total_budget`이 음수이거나 0인 경우 "1원 이상의 예산을 입력해주세요" 에러로 처리
  - 슬라이더(⑤ 선호도)는 기본값(3)이 있으므로 "비어있음"으로 간주하지 않음 (필수지만 항상 값이 존재)

### 5-2. 예산 및 인원을 고려했을 때 구매 가능한 좌석이 없는 경우

- **트리거 조건**: 기능 명세 4)의 5번 단계(예산 필터링) 결과, 조건을 만족하는 좌석 구역이 0개
- **동작**
  1. 이는 시스템 에러가 아닌 정상 처리 결과로 취급 (에러 로그/에러 화면 아님)
  2. 결과 영역에 다음을 표시:
     - 안내 문구: "입력하신 예산으로는 조건에 맞는 좌석이 없어요."
     - **최소 필요 예산 가이드**: 전체 좌석 구역(응원팀 조건 반영 후) 중 `party_size` 기준 총액이 가장 저렴한 구역의 가격을 계산해 "최소 {최소필요총액}원부터 예매 가능해요 ({구역명})" 형태로 노출
  3. `total_budget` 입력 필드로 돌아가는 "예산 다시 입력하기" 버튼/링크 제공, 클릭 시 해당 입력 필드로 포커스 이동
  4. TOP 3 카드 UI는 렌더링하지 않는다 (빈 카드나 0원 카드로 표시하지 않음)

### 5-3. 응답(연산)이 오래 걸리는 경우

- **트리거 조건**: `좌석 추천받기` 클릭 후 결과가 일정 시간(예: 2초) 이상 반환되지 않음
- **동작**
  1. 버튼 클릭 즉시 버튼을 비활성화하고 로딩 상태로 전환 (예: "추천 좌석 찾는 중..." 텍스트 + 로딩 인디케이터), 중복 클릭으로 인한 중복 연산 방지
  2. 일정 시간(예: 8~10초) 이상 지연 시, 로딩 UI 하단에 "예상보다 시간이 걸리고 있어요. 잠시만 기다려주세요." 보조 메시지 추가 노출 (사용자가 멈춘 것으로 오인하지 않도록)
  3. 최대 대기 시간(예: 15~20초)을 초과하면 5-4(응답 실패)와 동일한 타임아웃 실패 처리로 전환
  4. 로딩 중에는 입력 필드 값 변경이 즉시 반영되지 않도록 입력 영역을 비활성화(또는 변경 시 "결과를 다시 받으려면 재요청이 필요합니다" 안내)하여 입력값-결과 불일치를 방지

### 5-4. 추천 로직/응답이 실패하는 경우

- **트리거 조건**: 연산 중 예외 발생, 데이터셋 로드 실패, 타임아웃 초과, 알 수 없는 오류 등으로 추천 결과를 생성하지 못함
- **동작**
  1. 결과 영역에 에러 상태 UI 표시: "좌석 추천을 불러오지 못했어요. 다시 시도해주세요."
  2. `다시 시도` 버튼 제공 — 클릭 시 직전 입력값 그대로 유지한 채 추천 로직 재실행 (입력값 초기화하지 않음)
  3. 버튼/로딩 상태를 원상태로 복구하여 사용자가 재입력·재시도 가능하게 함
  4. 콘솔/내부 로그에는 실패 사유를 기록하되(개발용), 사용자 화면에는 기술적 에러 메시지(스택 트레이스, 에러 코드 원문 등)를 노출하지 않는다
  5. 동일 입력으로 재시도 2회 이상 연속 실패 시, 메시지를 "일시적인 문제가 계속되고 있어요. 잠시 후 다시 시도해주세요."로 변경하여 사용자가 반복 실패를 인지하도록 함

### 5-5. 입력값이 형식/범위를 벗어난 경우 (비어있지 않지만 유효하지 않은 값)

- **트리거 조건**: 값은 입력되었으나 허용 범위를 벗어남
  - `party_size`가 40 초과 (좌석 데이터상 처리 가능한 최대 인원 초과) 또는 소수/문자 입력
  - `total_budget`에 숫자가 아닌 문자, 음수, 비정상적으로 큰 값(예: 10억 초과 등 비현실적 값) 입력
  - `pref_cheer/view/comfort`가 슬라이더 조작 오류 등으로 0~5 범위를 벗어난 값이 들어온 경우
- **동작**
  1. 추천 로직 실행 전 클라이언트 단에서 값을 0~허용 범위로 clamp(보정)하거나, 보정이 애매한 경우(문자 입력 등) 인라인 에러로 반려
  2. 인라인 에러 메시지 예: "인원은 최대 40명까지 입력 가능합니다", "예산은 숫자만 입력해주세요"
  3. 5-1과 동일하게 결과 영역은 기존 상태 유지, 추천 로직은 실행하지 않음

### 5-6. 좌석 데이터셋 자체에 문제가 있는 경우 (해당 조건에 맞는 구역이 마스터 데이터에 아예 없음)

- **트리거 조건**: 응원팀·평일/주말 조합에 대응하는 좌석 데이터가 데이터셋에 정의되어 있지 않음 (데이터 누락)
- **동작**
  1. 이는 "예산 부족"(5-2)과는 다른 케이스이므로 별도 메시지로 구분: "현재 조건에 대한 좌석 정보를 찾을 수 없어요. 다른 조건으로 다시 시도해주세요."
  2. 예산 초과 안내(5-2)와 혼동되지 않도록 최소 필요 예산 가이드 문구는 노출하지 않는다
  3. 개발 단계에서는 이 케이스가 발생하지 않도록 성공 조건 1)에 따라 전체 조합(홈/어웨이 × 평일/주말)에 대한 데이터 커버리지를 사전 검증한다

---

## 6) 완료 조건 (Done Criteria)

이 화면/기능이 "완료"되었다고 판단하기 위한 체크리스트:

- [ ] 단일 화면에서 입력→실행→결과 확인까지 페이지 이동 없이 완결된다
- [ ] ①~⑤ 5개 입력 항목이 명세된 타입·범위대로 정상 동작한다
- [ ] 좌석 마스터 데이터의 구역/블록/평일가/주말가가 실제 KIA 챔피언스 필드 예매 기준과 100% 일치한다
- [ ] 정상 조건 입력 시 TOP 3 좌석(구분·블록·예상 총금액·추천 이유)이 매칭 점수 기준으로 정확히 산출된다
- [ ] 홈/어웨이 조건이 추천 결과에 실제로 다르게 반영된다
- [ ] 평일/주말 조건에 따라 가격이 올바르게 적용된다
- [ ] 예산 초과로 후보가 0개일 때 에러가 아닌 안내+최소 필요 예산 가이드가 노출된다 (5-2)
- [ ] 입력값 누락 시 5-1 규칙대로 인라인 에러가 노출되고 로직이 실행되지 않는다
- [ ] 입력값이 범위를 벗어날 때 5-5 규칙대로 보정 또는 에러 처리된다
- [ ] 처리 지연 시 로딩 상태 및 지연 안내가 5-3 규칙대로 표시된다
- [ ] 추천 로직 실패 시 5-4 규칙대로 재시도 가능한 에러 화면이 표시되고 입력값이 보존된다
- [ ] 데이터셋 누락 케이스가 5-6 규칙대로 예산 초과 케이스와 구분되어 처리된다
- [ ] 로그인, 회원가입, DB, 결제/예매 연동, 실시간 좌석 API, AI 챗봇, 인터랙티브 좌석도, 이력 저장 기능이 전혀 포함되어 있지 않다
